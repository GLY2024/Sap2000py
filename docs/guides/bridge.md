# Bridge components

The `sap2000py.bridge` package provides a library of parametric bridge
components (piers, girders, bearings, foundations), automatic connection
between them, and full-system assemblers. Requires the `bridge` extra.

!!! note
    Implemented in milestone M4. This page will document the component
    catalogue, the YAML parameter files, and the auto-connection protocol.
