# Enums & values

The single source of truth for OAPI integer values (units, item types, material
and load-pattern types) and the DOF mask helpers.

::: sap2000py.enums
    options:
      show_root_heading: false
      heading_level: 2
      members_order: source
