# Handles

Object creators return typed, immutable handles that stringify to the object's
name. Any API that takes an object accepts a handle or a raw name string.

::: sap2000py.handles
    options:
      show_root_heading: false
      heading_level: 2
      members_order: source
      show_if_no_docstring: true
